# cost-router 第二块砖：粘贴轨迹即算

静态形态评审版，未部署。直接打开 `web/index.html`，无需 build 或服务。

## 一屏形态（描述代替截图）

本机项目的 Node 与 Python 均无 Playwright，未生成截图、未做浏览器视觉验收。

深色中性背景、系统字体、直角边框，无渐变、emoji、投影或装饰卡片。顶部是“一份 agent 轨迹，四档成本。”桌面左侧为拖放区、选文件、载入合成示例及日志粘贴区；右侧为四档账单、三行路由建议、unknown 原因/逐请求收据和价格来源折叠。900px 以下改为上下排列，账单可横向滚动；手机适配已实现，尚未真机验收。

首屏预填 `samples/synthetic.jsonl`，共三次尝试，含一次失败及一次重试：

| USD | GPT-6 Sol | GPT-6 Luna | Opus 5.5 | Opus 5.5 Fast |
| --- | ---: | ---: | ---: | ---: |
| input | 0.012 | 0.0006 | 0.024 | 0.048 |
| cache read | 0.0036 | 0.00018 | 0.0036 | 0.0072 |
| cache write | 0.015 | 0.00075 | 0.03 | 0.06 |
| output（含 reasoning） | 0.03 | 0.0015 | 0.06 | 0.12 |
| 工具费用 | 0 | 0 | 0 | 0 |
| 完整总额 | 0.0606 | 0.00303 | 0.1176 | 0.2352 |

缺失项灰色显示 unknown，并提供原因；已知小计另列。示例路由三行：保留原模型；Luna 为固定轨迹最低完整成本情景；补齐同任务质量、时延及来源证据后再判断切换。

## 怎么用（三步）

1. 打开页面，拖入已有 JSONL/CSV，或粘贴日志；也可一键重载合成示例。
2. 对照四档逐项账单，展开 unknown 原因和请求收据，查看失败、重试及上下文档位。
3. 阅读三行路由建议，展开“价格快照 2026-09-24，每字段来源”复核 URL、抓取时间与说明。

## 为什么不是表单

用户交付的是运行 agent 后已经存在的日志文件，无需手填 token 数、缓存比例、模型价格或失败次数。导入即计算；不完整日志也能揭示缺口，不要求先补完一张表才能看到结果。这一形态是否有人愿意使用，仍待真实用户验证。

当前兼容 CLI 通用 JSONL/CSV 与已有 Codex 记录结构。Claude 用量需先符合通用字段，暂不支持直接导入 Claude 原生日志；不能把“已有日志”宣传为任意格式通吃。

## 第一个用户从哪来

搜索入口假设：`Opus Fast cost`、`agent cost per task`。第一批验证对象是已经保存用量日志、想复算单任务成本的开发者。形态通过后可从项目 README 放工具链接，并向 OpenRouter 社区展示一份脱敏轨迹的逐项复算。本轮未修改 README、未发帖，也未验证这些渠道的转化。

## 不做什么

不部署、不 push、不改 CLI；不更新价格快照、不接账户、不上传或保存日志、不加依赖。金额是固定 token/cache/retry 的 API 重放情景，不代表订阅扣款或实际节省。没有质量与时延证据，不宣称可以降级；缺数据不给完整假总额。

## 验证与范围记录

`calc_parity=pass`：新增一条 Python/JavaScript 对照测试，覆盖 20 组有效或异常输入，比较完整账单、路由及规范化记录，并核对内嵌价格和样本。全部 34 项测试通过。Node 页面运行检查通过：预填、粘贴、CSV、文件读取、拖放、错误态、unknown 和来源展示；该检查使用 DOM 桩，不能替代真实浏览器验收。

`page_lines=39`，口径为 `web/index.html` 行数。计算在 `web/calc.js`，交互在 `web/app.js`，历史快照与示例内嵌于 `web/data.js`，可离线打开。

范围外发现：README 仍写“本期没有网页”，按本轮边界只记录，未修改。未启动浏览器、服务或后台进程，无本轮进程及标签页遗留。
